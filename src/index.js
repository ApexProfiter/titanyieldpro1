import React from "react";
import ReactDOM from "react-dom/client";
import TitanYieldApp from "./TitanYieldApp";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <TitanYieldApp />
  </React.StrictMode>
);
